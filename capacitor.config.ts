import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'enchere.app',
  appName: 'mobile_enchere',
  webDir: 'build',
  bundledWebRuntime: false
  // server:{url:'http://192.168.43.45:8100'}
};

export default config;